[project]
name = "ethicaletfvalidator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "eliott", email = "eliott.thomas99@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain-openai>=1.2.1",
    "langgraph>=1.1.9",
    "pytest>=9.0.3",
    "pytest-mock>=3.15.1",
    "python-dotenv>=1.2.2",
    "tavily-python>=0.7.23",
    "yfinance>=1.3.0",
]

[build-system]
requires = ["uv_build>=0.9.9,<0.10.0"]
build-backend = "uv_build"
